GROUP BY

1. Contare quanti iscritti ci sono stati ogni anno:
SELECT YEAR(`enrolment_date`), COUNT(`enrolment_date`) AS `enrolled_same_year` FROM `students` GROUP BY YEAR(`enrolment_date`);

2. Contare gli insegnanti che hanno l'ufficio nello stesso edificio:
SELECT `office_address`, COUNT(`id`) AS `same_address` FROM `teachers` GROUP BY `office_address`;